NODE_ENV=development
# Porta HTTP desta API (a porta do Postgres fica só na DATABASE_URL, em geral :5432)
PORT=3333
DATABASE_URL=postgres://postgres:root@localhost:5432/orders
# Dados demo: use o script npm run dev:usedata (define USE_DEV_SEED=true).
